# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/gurupos?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-use-openssl-rand-base64-32
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3000
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env
# 2. Replace MONGODB_URI with your MongoDB Atlas connection string
# 3. Generate a secure JWT_SECRET: openssl rand -base64 32
# 4. Never commit the .env file to version control
