### 1. Clone and Install
```bash
zip file 
npm install --legacy-peer-deps
```
hiii
### 2. Run the app in development mode
```bash
npx expo start
```

 scan the QR code to open the app in your phone  -- app name - to scan the qr code--- expo app on play store in android device

 








###3 Run the app on android device or emulator
```bash
npx expo run:android
```
###4 -- for now do not  do that --  Run the app on ios device or simulator
npx expo run:ios